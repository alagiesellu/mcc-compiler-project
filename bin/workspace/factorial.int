// the factorial function

int x;
int y;

int f(int n) {
  x=y=1; 
  if (!(n > 0)) { 
    return 1; 
  } else {
    return n * n * f(n-1);
  }
}

for(y = 0; y < 5; y = y + 1){
	print(x = f(y));
	continue;	
}